server:
  port: 8080

spring:
  application:
    name: account
  profiles:
    active: "prod"
  config:
    import: "optional:configserver:http://localhost:8071/"
  datasource:
    url: jdbc:mysql://localhost:3306/accountdb
    username: root
    password: system@123
  jpa:
    show-sql: true
  sql:
    init:
      mode: ALWAYS

management:
  endpoints:
    web:
      exposure:
        include: "*"